<div class="t-gallery-narrow">
  <header>
    {{ render '@skipnav' }}
    {{ render '@header-logo' }}
  </header>
  {{ render '@breadcrumb' }}
  <main id="maincontent">
    <h2>Photo / Video Gallery for Narrow Cards</h2>
    {{#each card}}
      {{> @card }}
    {{/each}}
  </main>
  {{ render '@footer' }}
</div>
