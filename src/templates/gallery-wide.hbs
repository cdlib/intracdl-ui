<div class="t-gallery-wide">
  <header>
    {{ render '@skipnav' }}
    {{ render '@header' }}
  </header>
  {{ render '@breadcrumb' }}
  <main id="maincontent">
    <h1>Photo / Video Gallery for Wide Cards</h1>
    {{#each card}}
      {{> @card }}
    {{/each}}
  </main>
  {{ render '@footer' }}
</div>
