<section class="c-card">
  <a href="">
    <h2>{{ card.title }}</h2>
    <img src="{{ card.image }}" alt="Nature scene">
  </a>
  <div class="c-card__caption">
    <p>{{ card.caption }}</p>
  </div>
</section>
